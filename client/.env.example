# Leave REACT_APP_API empty when using Nginx reverse proxy on EC2 (recommended),
# so API calls route relatively to /api.
# If connecting directly to port 5000 from separate origin, specify: http://<EC2_PUBLIC_IP>:5000
REACT_APP_API=
